[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
# `sift` is taken on PyPI, so the distribution is sift-query. The command, the
# import and the repository all stay `sift`; only the pip install line differs.
name = "sift-query"
# Read from sift/__init__.py, so the version the CLI prints and the version on
# the distribution cannot drift apart. They already had: the package said 0.2.0
# while `sift --version` was about to ship saying something else.
dynamic = ["version"]
description = "Query CSV and JSONL from the terminal with a tiny SQL-like language. Streaming, zero dependencies."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Martin Muskov", email = "martinkmuskov@gmail.com" }]
keywords = ["csv", "jsonl", "query", "sql", "data", "cli", "streaming"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Environment :: Console",
    "Topic :: Utilities",
    "Topic :: Scientific/Engineering :: Information Analysis",
    "Typing :: Typed",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/martin-k-m/sift"
Repository = "https://github.com/martin-k-m/sift"

[project.scripts]
sift = "sift.cli:main"

[dependency-groups]
# The tools the tests and the lint gate need, in one place.
#
# Without this, `uv sync` installed nothing beyond the (empty) runtime
# dependencies and `uv run pytest` failed with "Failed to spawn: pytest" — the
# command the repository's own instructions tell you to run. CI worked because
# it named the same tools again in its own pip lines, which is two lists with
# no way of staying equal.
dev = ["pytest>=8", "ruff>=0.6"]

[tool.hatch.version]
path = "sift/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["sift"]

[tool.ruff]
# Pinned rather than inferred from requires-python. This is the line that makes
# the 3.9 floor a checked fact instead of a claim in the metadata: pyupgrade
# rules will not suggest 3.10+ syntax, and will flag it if someone writes it.
target-version = "py39"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
